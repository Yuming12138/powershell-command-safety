interface:
  display_name: "PowerShell Command Safety"
  short_description: "Safer PowerShell commands and interop"
  default_prompt: "Use $powershell-command-safety to write and run these PowerShell commands safely."
